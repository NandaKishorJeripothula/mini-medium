//The Initial State 
const initialState = {
    //As no user logged in,its empty
    session: {},
}
export default function reducer(state = initialState, action) {
    switch (action.type) {

    }
    return state;
}